import random

"""
# Definimos los parámetros
id_estanteria = 6
num_insertions = 100  # Cambia esto por el número de inserciones que quieras

# Generamos los inserts
for id_producto in range(38, 38 + num_insertions):
    cantidad = random.randint(11, 20)
    print(f" ({id_estanteria}, {id_producto}, {cantidad}),")


# Definimos los parámetros
id_estanteria = 6
num_insertions = 100  # Cambia esto por el número de inserciones que quieras

# Generamos los inserts
for id_producto in range(248, 248 + num_insertions):
    cantidad = random.randint(45, 52)
    print(f" ({cantidad}, {id_producto} ),")


"""
# Definimos los parámetros
id_estanteria = 8
num_insertions = 50  # Cambia esto por el número de inserciones que quieras

# Generamos los inserts
for id_producto in range(38, 38 + num_insertions):
    cantidad = random.randint(11, 20)
    print(f" ({id_estanteria}, {id_producto}, {cantidad}),")